# PostgreSQL Database Configuration
POSTGRES_USER=userhub
POSTGRES_PASSWORD=userhub123
POSTGRES_DB=userhub
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# PgAdmin Configuration
PGADMIN_EMAIL=admin@userhub.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Database Connection String
# For use in applications (adjust host for docker: use 'postgres' instead of 'localhost')
DATABASE_URL=postgresql://userhub:userhub123@localhost:5432/userhub

# Async database URL (for FastAPI with asyncpg)
DATABASE_URL_ASYNC=postgresql+asyncpg://userhub:userhub123@localhost:5432/userhub

# IMPORTANT: Change these passwords in production!
# Generate strong passwords using: openssl rand -base64 32
